import shelve
# f = shelve.open('shelve_demo')
# f['key'] = {'k1':(1,2,3),'k2':'v2'}
# f.close()

# f = shelve.open('shelve_demo')
# content = f['key']
# f.close()
# print(content)

# shelve  如果你写定了一个文件
# 改动的比较少
# 读文件的操作比较多
# 且你大部分的读取都需要基于某个key获得某个value
