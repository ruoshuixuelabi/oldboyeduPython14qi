"""
int
    bit_length() 二进制长度
    
bool
    类型转换
    False: 0, None, "", [], {}, tuple(), set()
    
    基本数据类型:
    x => y  y(x)

str
    索引和切片
    [start:end:step] 顾头不顾尾
    
    常用操作:
        1.strip() 去掉空白 \n \t 空格
        2.upper()  大写
        3.split() 切割, 默认用空白切割
        4.replace()  替换
        5.join() 拼接
        6.count() 计数
        7.find() 查找
        8.index() 索引查找
        9.startswith() 以xxx开头
        10. len() 长度
        
    for c in s:
        print(c)

"""
